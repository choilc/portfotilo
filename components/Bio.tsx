import { Box } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const BioItem = styled(Box)`
  display: flex;
  gap: 10px;
`;

export const YearBio = styled.span`
  font-weight: bold;
`;
